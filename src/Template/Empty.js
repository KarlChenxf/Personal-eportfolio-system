/**
 * The content of the template;
 * A typical content looks like:
 *  {
 *   components: [],
 *   layouts: [],
 *   page: {},
 *  }
 * 
 *  It is a template with no content.
 */
const content = {};

/**
 * The matedata for templates;
 * Templates should have a UNIQUE name,
 * a preview (image) and its content.
 */
export const Empty = {
    name: "Empty",
    preview: null,
    content: content,
};
